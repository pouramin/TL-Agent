# Internal engineering docs

- [`ARCHITECTURE.md`](./ARCHITECTURE.md) describes the current TL Agent launcher, browser workspace, process/Terminal foundation, Live Preview isolation, and local security boundaries.
- [`KILO_API_CONTRACT.md`](./KILO_API_CONTRACT.md) documents the pinned upstream runtime API contract and the production/current API surface TL Agent relies on.

Milestone-specific implementation notes should be folded back into these durable documents instead of accumulating as separate one-off files.
