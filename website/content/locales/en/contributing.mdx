---
title: Contributing
description: Contribute while preserving TL Agent's product, local-security, and runtime-abstraction boundaries.
icon: GitPullRequest
---

TL Agent application code and the public documentation site use separate build paths.

## Product development

The Go launcher and TL Agent-owned local APIs live under `cmd/launcher/`.

Browser application **source** lives in TypeScript under:

```text
cmd/launcher/ui/
```

Compiled browser JavaScript is emitted under:

```text
cmd/launcher/web/
```

Do not treat generated `web/*.js` as the primary source when changing product behavior.

Before proposing browser changes:

```bash
npm install
npm run check:web
npm run build:web
go test ./...
```

## Architecture rules

- Project filesystem/Search/process/Preview capabilities belong behind TL Agent's `/local/*` APIs.
- Agent execution concepts belong behind the TL Agent-owned `/runtime/*` contract.
- Browser feature modules must not construct implementation-specific engine routes.
- Provider/Model product definitions belong to TL Agent's registry, not to browser-local vendor config.
- New editor/runtime assets must remain bundled locally rather than loaded from a CDN.
- Preview project code must stay on a separate local origin from TL Agent's control API.

## Internal engineering docs

Engineering notes live under `docs/` in the main repository.

## Public documentation

The Fumadocs site lives under `website/`.

```bash
cd website
npm install
npm run typecheck
npm run build
```

Keep user-facing language centered on **TL Agent**. Name the bundled engine only in the maintainer compatibility reference or where legal attribution genuinely requires it.
